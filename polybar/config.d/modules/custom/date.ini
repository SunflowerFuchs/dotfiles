[module/date]
type = custom/script
interval = 1

exec = bash -c '/bin/date +"%a %Y-%m-%d %H:%M:%S"'

format-prefix = " "
format-prefix-foreground = ${colors.foreground-alt}
format-underline = #0a6cf5

click-left = gnome-calendar >/dev/null 2>&1 &
